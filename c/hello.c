#include <stdio.h>

int main() {
    char *s = "Hello, World!"; // C does not have a string type. Instead, it uses a char array.
    printf("%s", s);
    return 0; // Returning from the main function exits the program with the specified exit code.
}
